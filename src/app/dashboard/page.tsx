import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard | DevStash",
};

export default function DashboardPage() {
  // Placeholder main area — phase 3 fills this with stats, collections and items.
  return <h2 className="text-[15px] font-semibold">Main</h2>;
}
